#!/bin/bash
function calc {
    TZ="Australia/Brisbane" date +"%$1"
}

S=$(calc S)
M=$(calc M)
H=$(calc H)
D=$(calc d)
m=$(calc m)
Y=$(calc Y)

import -window root -frame -silent "/home/fusion809/Pictures/Screenshots/i3/Screenshot_${Y}${m}${D}_${H}${M}${S}.png"
